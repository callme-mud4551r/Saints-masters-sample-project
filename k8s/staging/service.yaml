apiVersion: v1

kind: Service

metadata:

  name: three-tier-app-service

spec:

  type: LoadBalancer

  selector:

    app: three-tier-app

  ports:

    - protocol: TCP

      port: 80

      targetPort: 5000
