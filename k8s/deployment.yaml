apiVersion: apps/v1
kind: Deployment
metadata:
  name: three-tier-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: three-tier-app
  template:
    metadata:
      labels:
        app: three-tier-app
    spec:
      containers:
      - name: flask-app
        image: 160686961672.dkr.ecr.ap-south-1.amazonaws.com/three-tier-app:latest
        imagePullPolicy: Always

        env:
        - name: DB_HOST
          value: "three-tier-app-mysql.cxes68wssrwe.ap-south-1.rds.amazonaws.com"

        - name: DB_USER
          value: "db_username"

        # Replace with your actual password
        - name: DB_PASSWORD
          value: "db_password"

        - name: DB_NAME
          value: "three_tier_app_db"

        ports:
        - containerPort: 5000
