apiVersion: apps/v1
kind: Deployment
metadata:
  name: three-tier-app
  namespace: production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: three-tier-app
  template:
    metadata:
      labels:
        app: three-tier-app
    spec:
      containers:
      - name: flask-app
        image: 622488711452.dkr.ecr.ap-south-1.amazonaws.com/three-tier-app:latest
        imagePullPolicy: Always

        env:
        - name: DB_HOST
          value: "three-tier-app-mysql.cnasygy06z8x.ap-south-1.rds.amazonaws.com"

        - name: DB_USER
          value: "db_username"


        - name: DB_PASSWORD
          value: "db_password"

        - name: DB_NAME
          value: "three_tier_app_db"

        ports:
        - containerPort: 5000
